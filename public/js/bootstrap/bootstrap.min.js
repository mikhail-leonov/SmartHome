/* PLACEHOLDER — vendor the real Bootstrap 5 JS bundle here for production/offline.
   Download: https://github.com/twbs/bootstrap/releases (dist/js/bootstrap.bundle.min.js)
   The landing page itself uses no Bootstrap JS components, so this stub is harmless.
   If you add modals/dropdowns/etc., replace this with the real bundle. */
